class Recipe {
  final String recipeId;
  // final String name;
  final String description;
  // final String ingredients;

  // const Recipe(this.recipeId, this.name, this.description, this.ingredients);
  const Recipe(this.recipeId, this.description);

  // Recipe.fromJson(Map<String, dynamic> json, this.id, this.name,
  //     this.description, this.ingredients);

  // Map<String, dynamic> toJson() => {
  //       "id": id,
  //       "name": name,
  //       "description": description,
  //       "ingredients": ingredients
  //     };
}
